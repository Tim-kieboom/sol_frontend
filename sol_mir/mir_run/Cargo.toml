[package]
name = "mir_run"
version = "0.1.0"
edition = "2024"

[dependencies]
ast_model = { version = "0.1.0", path = "../../sol_ast/ast_model" }
mir_model = { version = "0.1.0", path = "../mir_model" }
mir_parser = { version = "0.1.0", path = "../mir_parser" }
sol_utils = { version = "0.1.0", path = "../../sol_utils" }

[dev-dependencies]
ast_parser = { version = "0.1.0", path = "../../sol_ast/ast_parser" }
ast_run = { version = "0.1.0", path = "../../sol_ast/ast_run" }
sol_resolver = { version = "0.1.0", path = "../../sol_ast/sol_resolver" }
sol_tokenizer = { version = "0.1.0", path = "../../sol_tokenizer" }
